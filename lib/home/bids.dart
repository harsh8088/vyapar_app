import 'package:flutter/material.dart';

class Bids extends StatelessWidget {
  const Bids({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Bids');
  }
}